import { PlaceholderSettingsPanel } from "@/components/settings/placeholder-settings-panel";

export default function AccessibilitySettingsPage() {
  return (
    <PlaceholderSettingsPanel
      title="Accessibility Features"
      description="Controls that make the workspace easier to read, navigate, and operate."
      note="Accessibility preferences are being added here so this becomes a practical control surface rather than just another static page."
    />
  );
}
